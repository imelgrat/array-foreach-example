{
  "name": "arrayforeach-example",
  "version": "1.0.0",
  "description": "The Array.forEach() method is one of the oldest ones available and allows calling a function (a callback or arrow function) once for each array element.",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1"
  },
  "keywords": [
    "javascript", "arrays", "iteration", "array foreach", "foreach"
  ]
}